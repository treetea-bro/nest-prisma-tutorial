services:
  db:
    container_name: db_prod
    restart: always
    env_file:
      - .env.prod

  app:
    container_name: app_prod
    restart: always
    build:
      context: .
      dockerfile: Dockerfile.prod
    env_file:
      - .env.prod
